Minha checklist: 

1. AFD (primeiro passo)

    [X] Liste todos os tokens da linguagem (palavras reservadas, identificadores, números, operadores, símbolos).

    [X] Desenhe o AFD em Graphviz (ou JFLAP).

        Estados iniciais e finais.

        Transições para identificadores, números, operadores e símbolos.

        Estado de erro para caracteres inválidos.

    [X] Gere a imagem do diagrama (dot -Tpng afd.dot -o afd.png).

    [X] Inclua o diagrama no relatório.

2. Estruturas em C

    [X] Crie struct Token com nome, lexema, linha e coluna.

    [X] Crie a Tabela de Símbolos (array ou lista).

        Inicialize com palavras reservadas.

        Insira identificadores conforme aparecem.

        Normalize para minúsculo (case-insensitive).

3. Implementação do Analisador Léxico

    [X] Abra o arquivo fonte .pas com fopen().

    [X] Leia caractere por caractere com fgetc().

    [X] Controle linha e coluna.

    [X] Função proximoToken() que retorna o próximo token.

    [X] Ignore espaços, tabulações, quebras de linha e comentários { ... }.

    [X] Reconheça tokens básicos:

        Identificadores e palavras reservadas.

        Números inteiros e reais.

        Operadores e símbolos.

4. Tratamento de Erros

    [X] Detecte caracteres inválidos (@, %, $).

    [X] Detecte comentário não fechado.

    [X] Detecte string não fechada (se aplicável).

    [X] Registre no arquivo .err → tipo do erro, linha e coluna.

5. Saída do Analisador

    [X] Gere arquivo .lex com tokens reconhecidos.

    [X] Gere arquivo .ts com tabela de símbolos final.

    [X] Gere arquivo .err com erros léxicos.

6. Relatório Técnico

    [ ] Descrição das estruturas de dados.

    [ ] Descrição das funções implementadas.

    [ ] Explicação do AFD (com diagrama).

    [ ] Testes realizados (mínimo 3 corretos e 3 com erro).

    [ ] Saídas geradas pelo analisador.

    [ ] Conteúdo final da tabela de símbolos.

7. Analisador Sintático (Implementado)

    [X] <programa>
    [X] <bloco>
    [X] <parte de declarações de variáveis>
    [X] <declaração de variáveis>
    [X] <lista de identificadores>
    [X] <tipo>
    [ ] <comando composto> (Nota: `parse_compound_command` está parcialmente implementado, mas precisa de revisão na condição do loop e remoção da recursão indevida)

8. Próximos Passos - Analisador Sintático (Concluídos)

    [X] Implementar `<comando>` (roteamento feito usando `peek()`)
    [X] Implementar `<atribuição>`
    [X] Implementar `<comando condicional>`
    [X] Implementar `<comando repetitivo>`
    [X] Criar e implementar função para `<expressão>`
    [X] Criar e implementar função para `<relação>`
    [X] Criar e implementar função para `<expressão simples>`
    [X] Criar e implementar função para `<termo>`
    [X] Criar e implementar função para `<fator>`
    [X] Criar e implementar função para `<variável>` (Embutida em fator/atribuição)
